<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pratice2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pratice2">
        <activity android:name=".viewBinding.ViewBindingPratice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.ViewModelActivity">
        </activity>
        <activity android:name=".DataWithLiveActivity" />
        <activity android:name=".dataBindg.DataBindingActivity" />
        <activity android:name=".liveData.LiveData" />
        <activity android:name=".lifecycle.LifeCycleActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>